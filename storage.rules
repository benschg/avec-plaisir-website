rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Gallery images - publicly readable, authenticated users can upload
    match /gallery/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
